<%= flash_helper %>

<div class="querybar">
  <h1>And... here you go!</h1>
  Here is a wines list based off of your requirements. In our calculations, we assume that each person will drink <strong><%= @glassPerPerson %></strong> five-ounce glasses of wine.<br>
  You will need <%= @bottles.ceil %> bottle(s) with a suggested price per bottle of $<%= @pricePer.round(2) %><br>
  <br/><br/>

  <strong>Wine List:</strong>
  <ol>
    <% @json["Products"]["List"].each do |x| %>
      <%= print_wine_json(x) %>
    <%end%>
  </ol>
  <% @result %>
</div>
